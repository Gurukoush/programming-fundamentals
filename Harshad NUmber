class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        count = 0
        for i in str(x):
            count+=int(i)
        if x%count==0:
            return count
        else:
            return -1
